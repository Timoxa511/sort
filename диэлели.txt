короче,    такая тема с длл, мой основной .exe импортит в себя все длл, что найдет в папке
а в дллях(оговорено) лежат функции сортировок массивов. Для их работы мне нужна int_t.h,
но если я просто ее провключаю в каждую длл, то int_t.h прокопируется тучу раз. Если я разобью .h на
.h с прототипами и lib*.a с телами, то на этапе линковки дллей не все, но как минимум все прототипы и часть функций 
прокопируется тоже. ПОЭТОМУ! я создам длл из .h, которую будут включать все остальные длли, в таком случае,
загрузчик windows, загрузив в память int_t.dll в первый раз для первой длли, в следующие разы будет обращаться к этой же области памяти 
и копирования не будет(благо крутому загрузчику windows)


                                   1 динамически --->  LoadLibrary + GetProcAddress       (WinAPI)
есть два способа работы с дллей: 
								   2 статически ---->  создавая импортную либу(.a):
									  g++ -c <name>.cpp (компелировать, но не линковать)  создаст <name>.o     
									  g++ -shared -o <name>.dll -Wl,--out-implib,lib<name>.a <name>.o

								    потом, на этапе сборки экзешника, нужно будет прилинковать либу ключом -l с параметром <name>
или, в настройках компилятора 
в codeblocks, добавить либ в ---------> g++ -o program.exe -l<name> -L. program.cpp
список линковки               
                              			           			       ^
			    									    	       |
											чтобы искал не только в папке компилятора minGW/../lib, 
														 	но и в текущей
															 


есть еще оккультный трюк, которым пользуются в винде: обмена интерфейсами
идея в том, что все функции длл-и превращают в виртуальные функции одного класса, тогда создаются таблицы указателей 
и для доступа ко всем функциям длли из основоного файла нужно будет проэкспортить только крейтор функцию. Так как функции класса виртульные, то
вызывая функции полученного обьекта по указателям к ним будет доступ. Теперь в крейтор можно принимать параметром какой-нибудь объект 
экзешниковского класса и использовать его виртуальные функции в длли
magic 
единственное требование - ашник с объявлением общих классов